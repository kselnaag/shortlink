run:
  concurrency: 1
  timeout: 10m

linters:
  disable-all: true
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - goimports
    
issues:
  exclude-rules:
  - path: /
    linters:
    - typecheck
